import { AbstractDebounceDirective } from './abstract-debounce.directive';

describe('AbstractDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
